# La Fiesta — Demo Site (Next.js + Tailwind)

**How to run locally**
```bash
npm install
npm run dev
```
Then open http://localhost:3000

**Deploy to Vercel**
1) Push this folder to a new GitHub repo.
2) On https://vercel.com → New Project → Import your repo → Deploy.
